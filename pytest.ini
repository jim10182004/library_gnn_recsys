[pytest]
# 避免 Windows OneDrive 路徑下 pytest cache 權限錯誤
addopts = -p no:cacheprovider
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# 標籤
markers =
    slow: 標記需要 GPU 或大量資料的測試
    integration: 需要訓練好的 checkpoint 才能跑的整合測試
